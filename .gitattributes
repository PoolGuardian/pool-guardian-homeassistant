# Normalise to LF in the repository regardless of contributor platform.
# Without this, a Windows checkout can commit CRLF into the YAML workflows and
# Python sources, which shows up as whole-file diffs and breaks nothing locally
# but is miserable to review.
* text=auto eol=lf
